// CORRECT — Tailwind v3 style!
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}

// Uses tailwindcss package directly ✅
// Uses autoprefixer package directly ✅
// Both are in package.json ✅
// No extra packages needed! ✅